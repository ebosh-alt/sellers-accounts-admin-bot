Введите новый тип аккаунта

За 1 раз можно ввести только один тип аккаунта, проверьте чтобы не было лишних символов в конце строки(пробелы, запятые,
переносы строк)

{% for acc_type in acc_types %}`{{acc_type}}`  
{% endfor %}